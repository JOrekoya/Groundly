[project]
name = "groundly"
version = "0.1.0"
description = "Real estate deal analyzer with a deterministic finance core."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["backend"]
testpaths = ["backend/tests"]
